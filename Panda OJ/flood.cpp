#include <bits/stdc++.h>

int main(){
    int tc;
    scanf("%d", &tc);
    while(tc--){
        int n, m;
        scanf("%d %d", &n, &m);
        char flood[n+1][m+1];
        memset(flood, 0, sizeof flood);
        for(int i=0;i<n;i++){
            for(int j=0;j<m;j++){

            }
        }
    }
}
