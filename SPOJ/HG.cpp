#include <iostream>
#include <stdio.h>
using namespace std;

string mult(string a, string b){
    string res="";
    if(a.size()<b.size()){
        string temp=a;
        a=b;
        b=temp;
    }
    int carry=0, temp, sum;
    for(int i=0;i<)
}

int main(){
    int n;
    scanf("%d", &n);
    string ans;
    while(n--){

    }
}
