#include <bits/stdc++.h>
using namespace std;

bool pagar(int x, int y, int ax, int ay){
    if(x == ax && y == ay) return false;
    return true;
}

int main(){
    char arr[6][6];
    pair<int, int> pos;
    queue<pair<int, int> > st, curr;
    set<int, int> flag;
    while(!st.empty()){
        st.pop();
        x =
        if()
    }

}
