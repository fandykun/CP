#include <bits/stdc++.h>
using namespace std;

int main(){
    int tc;
    scanf("%d", &tc);
    while(tc--){
        int x;
        scanf("%d", &x);
        printf("%d\n", (x+1)/2);
    }
    return 0;
}
